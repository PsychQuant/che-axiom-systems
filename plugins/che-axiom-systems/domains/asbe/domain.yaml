# Domain manifest — read by axiom-lookup / axiom-validate / axiom-create
# format:   yaml(結構化 ASBE schema) | markdown(散文公理) | freeform(自訂體系)
# maturity: bootstrapped(已符合 ASBE YAML schema，validate 跑 ERROR 級) | legacy(未遷移，validate 降級 WARNING)
domain: asbe
description: ASBE 方法論自身的公理化（self-bootstrap）
format: yaml
maturity: bootstrapped
entry_points:
  - asbe_axioms_bootstrapped.yaml
